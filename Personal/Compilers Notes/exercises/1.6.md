1. For the block-structured C code of **figure a**, indicate the values assigned to **w**, **x**, **y**, and **z**. Repeat for **figure b**
2. For the block-structured code of **figure c**, assuming the usual static scoping of declarations, give the scope for each of the twelve declarations.
3. What is printed by the following C code
```c
#define a (x+l)
int x = 2;
void b() {
    x = a ; 
    printf ("%d\n", x); 
}
void c () { 
    int x = 1; 
    printf("%d\n", a); 
}
void main() { 
    b(); 
    c();
}
```